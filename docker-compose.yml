services:
  db:
    image: postgres:16
    container_name: arena_db
    environment:
      POSTGRES_USER: arena
      POSTGRES_PASSWORD: arena_password
      POSTGRES_DB: arena
    ports:
      - "5432:5432"
    volumes:
      - arena_postgres_data:/var/lib/postgresql/data

volumes:
  arena_postgres_data: